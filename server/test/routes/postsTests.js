const assert = require("chai"),
      request = require("supertest"),
      server = require("../../server");

describe("routes/posts", () =>
{
    it("", () =>
    {
        // request(server).get
    });
});