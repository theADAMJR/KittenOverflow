import { TestBed } from '@angular/core/testing';
import app from "./app";

describe('App Tests', () => 
{
  beforeEach(() => 
  {
    TestBed.configureTestingModule({});
  });

  it('should be created', () => 
  {
  });
});