import { CleanDatePipe } from './clean-date.pipe';

describe('CleanDatePipe', () => {
  it('create an instance', () => {
    const pipe = new CleanDatePipe();
    expect(pipe).toBeTruthy();
  });
});
